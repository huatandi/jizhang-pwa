# 飞常明细 V224：语音、OCR 与提醒可靠性修复

日期：2026-09-10。基线：用户上传的 V223 完整 ZIP。构建 ID：`3afafbe7`。

本次完成源码修复和本地自动化门禁。未部署到用户现有站点，未操作真实账本，手机实机验收仍待执行。本报告不把解析器测试当成麦克风识别率，也不把文字样本测试当成图片 OCR 准确率。

## 1. 审查方法与范围

以首席架构师、高级全栈工程师、语音/OCR 数据可靠性及工程质量负责人视角，检查语音金额解析、提醒对话字段、OCR 主/回退管线、任务状态及 worker 生命周期、跨模态草稿合并、构建与缓存更新。先跑原包基线，再用反例定位缺陷，做有界修复，最后执行关联回归。不因已有 PASS 报告而跳过实际执行，不为凑轮次重复修改。

本次未增加新的模型、外部服务或云端依赖。保留原有模型、vendor、其他模块与历史资料；没有删除或迁移账本数据库。

## 2. 已复现并修复

| 模块 | 原有问题 | 修复结果 |
|---|---|---|
| 中文小数 | `三点五元` 得到 3；`三点五万` 得到 3 | 中文小数逐位转换，支持小数大单位及元后缀；分别得到 3.5、35000 |
| 千位分隔金额 | `1,300.50` 得到 1 | 仅移除合法三位分组的分隔符，得到 1300.5 |
| 提醒内容 | `事项联系同事` 被截成“联系同”，末字被当作未完成标签 | 普通词内部的单字不再被扣留；“准备”“工地”同样保留 |
| 提醒分句 | 先说“地点”，再说“移民局”，内容可能进入事项 | 已打开字段优先承接后续无标签话语，并规范化提醒方式值 |
| OCR 回退金额 | 没有 TOTAL 时取最大金额，付款额可能被当成总额 | 只接受明确总额标签；SUBTOTAL、IVA、EFECTIVO 不当总额。无证据或不同总额冲突时留空，提供原因 |
| OCR 回退商户 | 首行大写字被直接认作商户 | 取消该猜测；无标签时保留空值 |
| OCR 回退日期 | 2 月 31 日也能形成日期字段 | 校验实际日历日期，包括闰年；非法日期留空 |
| OCR worker | 已改为按语言缓存 worker，但 shutdown 仍释放旧的空变量 | 释放缓存中的全部 worker；重复关闭安全；识别中拒绝释放；后续识别重新创建 |
| OCR 任务 | 历史 Map 无限增长；结束后仍能触发进度回调；业务读取不存在的 aborted 属性 | 保留最多 32 个结束任务及全部运行任务；结束后不再更新进度；提供实际取消状态 |
| OCR 取消传播 | 取消被当作空结果，消费者可能继续启动旧引擎回退 | AbortError 继续向外传播，两个消费者在取消时停止；返回结果前再检查取消状态；后处理异常标记失败 |
| 草稿金额 | Infinity、空字符串、布尔值可变为异常金额或 0 | 非数值、非有限值及超出安全分精度的金额归一化为空；合法 0 保留 |
| 草稿明细 | 第一来源没有 items 却产生空数组，阻止后续来源补齐 | 空数组视为缺失，允许后续来源补齐；已有明细仍保持首来源优先 |
| 说明文档 | 仍写“多笔可编辑”“完全离线”，打包命令指向不对应的脚本 | 更新到当前行为和实际测试/构建命令 |

OCR 回退采取保守策略：没有明确总额时，即使出现 SUBTOTAL/IVA，也不在简易回退解析器中另造数学推断。复杂证据裁决仍由现有主管线处理。金额留空需要用户核对。

## 3. 验证证据

执行环境：Linux / Node v24.19.0。完整原始日志随包附带。

| 门禁 | 结果 |
|---|---|
| 原始基线 `npm test` | 73 个测试文件通过，0 失败，1 网络测试跳过 |
| 最终 `npm test` | 75 个测试文件通过，0 失败，1 网络测试跳过 |
| 新增识别安全测试 | 62 条行为断言通过 |
| 新增 OCR 取消运行时测试 | 2 个场景 / 6 条断言通过；使用实际工作台函数、模拟 OCR 引擎 |
| 原 V223 提醒边界测试 | 13/13 通过 |
| JavaScript 语法门禁 | 158/158 通过 |
| `npm run build` | 通过，构建 ID `3afafbe7` |
| `npm run verify` | 资源哈希、136 处页面版本引用及 189 项 APP_SHELL 检查通过 |

完整回归覆盖原有数据安全对账、数据库迁移、交易核心、语音状态/草稿、提醒清空/方式/日期、OCR 总额证据/约束/候选/日期/长票据/任务、模型资源及安全更新相关测试。通过这些门禁不等于全部功能均已真实浏览器或真机验证。

新增测试文件：`_test_v224_recognition_safety.cjs`、`_test_v224_ocr_cancel_runtime.cjs`。

## 4. 修改文件与更新方式

主要业务修改：

- `js/voice/voice-parser.js`
- `js/voice/reminder-dialogue-engine.js`
- `js/offline-ocr.js`
- `js/ocr/ocr-job-manager.js`
- `js/ai/ai-workbench.js`
- `js/validation/transaction.js`

构建器同时更新页面资源版本、Service Worker 缓存标识及构建清单。具体文件差异和逐文件 SHA256 见 `delivery-evidence/source-changes.json`；可读源码补丁见 `delivery-evidence/source.patch`。

升级步骤：

1. 在原应用导出账本备份，保留当前部署目录的副本。
2. 解压本完整包，将 `v224/` 内文件作为项目根内容，保持原目录结构。先在单独测试目录核验，再替换原站点静态资源。
3. 有 Node 18+ 时，执行 `npm test`、`npm run build`、`npm run verify`。仅运行现有静态站点不需要为本次修改新增 npm 依赖。
4. 保持原应用域名、协议和端口；不同来源对应不同浏览器本地数据，切换地址不能视为账本自动迁移。
5. 原有安全更新器继续负责缓存更新。结束正在编辑/识别的任务，再关闭并重新进入应用，核对加载构建 `3afafbe7`。不要为了更新清除站点数据或 IndexedDB。
6. 如果出现问题，恢复升级前静态文件。恢复后先核对数据；不要用旧备份覆盖升级期间新增的账目。

## 5. 必须保留的实机验收与限制

以下属于未验证项目，不能写成“全系统完美闭环”：

- 手机麦克风真实识别：中文单位、小数、口音、噪声、长时间录音及不同 Provider 的最终转写。
- 真实票据图像：模糊、反光、长票据、TOTAL 的粗体 `$`；本次没有新增用户票据照片作为实际 OCR 输入。
- iOS / Android 后台提醒、锁屏响铃、通知权限、电池优化及断网恢复。本次未改变操作系统的后台限制。
- 全流程取消还需要实机确认页面离开、重新上传、用户取消按钮均确实触发任务取消。此次修复任务状态及管线传播，不能据此保证每个 UI 入口都已接通取消。
- 提醒自然语言中“内容/位置/事项”等词同时可能是普通文本或字段标签，现有标签切分仍存在语义歧义；本次只修复已复现的单字截断和跨句字段归属，不声称解决所有自由表达。
- 语音已有“漏万恢复”逻辑仍按原契约保留；模糊语音不能仅凭后处理证明实际说出的数量级。高金额请核对草稿后保存。
- `_test_fx` 实时网络测试按项目原规则跳过；天气、汇率、模型在线下载未联网实测。
- 真实账本导入/导出、应用升级后的数据保留、性能和耗电需要用户设备验收；自动化中的模拟数据不是用户实际数据。

建议一次实机验收集中完成：录入“1万3千4百5十5 / 三点五元 / 支出三点五万元”，逐项核对草稿再保存并查询；提醒先说“地点”再说“移民局”；扫描含 SUBTOTAL/IVA/TOTAL/EFECTIVO/CAMBIO 的单据及无 TOTAL 单据；取消一次识别并确认没有继续回退写字段；切换中/英/西 OCR 后重复操作，确认无卡死。记录设备、构建 ID、输入、预期与实际结果。出现明确失败再做有界修复，不重复无证据调参。
