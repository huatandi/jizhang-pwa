# V209 Recognition + Theme Integrity Upgrade

## 本轮目标
- 语音金额“万”同音恢复加强；疑似“万”被完全吞掉时不静默按小金额写入。
- 首页 8 张财务 KPI 卡全部改用主题语义 Token，Autumn/Dark 不再串色。
- 保存/取消/成功/失败等 Toast 不再由 JS 写死颜色，自动跟随主题并保持高反差文字。
- 设置页显式提供“智能识别与模型管理”，TEN-VAD 与 OCR 安装/检测入口可被普通用户直接找到。

## 安全边界
- 不凭空把“疑似缺万”的数字自动放大；无法可靠恢复时要求重新说金额。
- 模型不静默下载；OCR 只有用户点击“安装 / 预加载 OCR”才主动预加载。
- TEN-VAD / OCR 真机准确率仍需 GitHub Pages + 实际麦克风/票据验证，单测不冒充真机结果。
